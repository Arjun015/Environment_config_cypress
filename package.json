{
  "name": "environment-config",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "stag:chrome":"npx cypress run --headed --browser chrome --config baseUrl=\"https://www.google.com/\"",
    "prod:electron":"npx cypress run --headed --browser electron --config baseUrl=\"https://www.airtel.com/\"",
    "loc:edge":"npx cypress run --headed --browser edge --config baseUrl=\"https://www.facebook.com/\"",
    "staging-config":"npx cypress run --headed --config-file staging-config.json",
    "Production-config":"npx cypress run --headed --config-file production-config.json",
    "local-config":"npx cypress run --headed --config-file local-config.json"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cypress": "^9.6.0",
    "npx": "^10.2.2"
  },
  "devDependencies": {
    "cypress-file-upload": "^5.0.8"
  }
}
